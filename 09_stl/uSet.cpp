#include<bits/stdc++.h>

using namespace std;

// TC: O(1) or O(n) in worst cases

void explainUSet(){
    unordered_set<int> ust;
    // Lower bound and upper bound function doesn't work
    // rest all the functions are same as above
    // it does not store it in any particular order
    // has better complexity then set, except some when collisions happens
}

int main(){
    explainUSet();
    return 0;
}